## Egypt Land Classification — Streamlit Deployment

## **Overview**
This application loads a fine-tuned CNN model and performs satellite land classification on EuroSAT + Egypt datasets.

## **How to run locally**
pip install -r requirements.txt
streamlit run app.py

## **Model**
Place finetuned_egypt_model.pth in the project directory.

